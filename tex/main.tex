\documentclass[amsmath,amssymb,aps,twocolumn,superscriptaddress,nofootinbib]{revtex4-1}
\pdfoutput=1
\usepackage{amsfonts, mathtools, mathrsfs,upgreek}
\usepackage{hyperref, csquotes}
\usepackage{graphicx, color}
\usepackage{tikz,pgf,tikz-cd,float}
\usepackage{physics,tensor,subcaption,multirow}
\usepackage{enumitem}

\newcommand{\diff}{\text{d}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\SUT}{\mathrm{SU}(2)}
\newcommand{\SUO}{\mathrm{SU}(1,1)}
\newcommand{\e}{\mathrm{e}}
\newcommand{\f}{\mathrm{f}}
\newcommand{\Sreg}{S_{\mathrm{R}}}
\newcommand{\caj}[1]{\textcolor{purple}{#1}}
\newcommand{\one}{\mathbb{1}}
\newcommand{\GN}{G_{\mathrm{N}}}
\newcommand{\bi}{\gamma_{\textsc{bi}}}
\newcommand{\defeq}{\vcentcolon=}
\newcommand{\eqdef}{=\vcentcolon}

\DeclareMathOperator*{\sumint}{%
\mathchoice%
  {\ooalign{$\displaystyle\sum$\cr\hidewidth$\displaystyle\int$\hidewidth\cr}}
  {\ooalign{\raisebox{.14\height}{\scalebox{.7}{$\textstyle\sum$}}\cr\hidewidth$\textstyle\int$\hidewidth\cr}}
  {\ooalign{\raisebox{.2\height}{\scalebox{.6}{$\scriptstyle\sum$}}\cr$\scriptstyle\int$\cr}}
  {\ooalign{\raisebox{.2\height}{\scalebox{.6}{$\scriptstyle\sum$}}\cr$\scriptstyle\int$\cr}}
}

\begin{document}

\title{From discretuum to continuum}

\author{Alexander F. Jercher}
\email{alexander.jercher@uni-jena.de}
\affiliation{Theoretisch-Physikalisches Institut, Friedrich-Schiller-Universit\"{a}t Jena,\\ Max-Wien-Platz~1, 07743 Jena, Germany, EU
}
\address{corresponding author}
\author{Sebastian Steinhaus}
\email{sebastian.steinhaus@uni-jena.de}
\affiliation{Theoretisch-Physikalisches Institut, Friedrich-Schiller-Universit\"{a}t Jena,\\ Max-Wien-Platz~1, 07743 Jena, Germany, EU
}

\date{\today}

\begin{abstract}
{
.
}
\end{abstract}

\maketitle

\section{Introduction}

$8\pi\GN = 1$?, spacetime signature $(+,-,-,-)$

\section{Setup}

\subsection{Lorentzian Regge calculus}

Regge calculus~\cite{Regge:1961ct} is a discrete gravitational theory on simplicial manifolds $\Delta$ with the length of edges $\{l_e\}$ dynamical.
In a Lorentzian setting, a simplicial manifold consists of intrinsically flat Lorentzian $4$-simplices $\sigma\in\Delta$. Curvature is located at triangles $t\in\Delta$ and captured by deficit angles $\delta_t(\{l_e\})$, lying in the space orthogonal to $t$.
For $t$ spacelike (timelike), this space is isomorphic to $\R^{1,1}$ ($\R^2$).\footnote{For the remainder, we neglect lightlike edges, triangles and tetrahedra.
See~\cite{Sorkin:2019llw} for the definition of deficit angles associated to $(d-1)$-dimensional null cells.} 
Lorentzian deficit angles are defined as $\delta_t^{\mathrm{L},\pm} = \mp i 2\pi  -\sum_{\sigma\supset t}\psi_{\sigma,t}^{\mathrm{L},\pm}$, following the conventions of~\cite{Sorkin:2019llw,Asante:2021zzh}, where the $\psi_{\sigma,t}$ are dihedral angles.
\enquote{$\pm$} indicates a choice of sign which becomes important for causally irregular configurations~\cite{Sorkin:2019llw,Asante:2021zzh,Asante:2021phx} discussed below.
The bulk action of Lorentzian Regge calculus is given by~\cite{Asante:2021zzh,Sorkin:2019llw,Regge:1961ct}
%
\begin{equation}\label{eq:general Regge action}
\begin{aligned}
\Sreg[\{l_e\}] =& \sum_{t\in\Delta:\text{ tl}}\abs{A_t(\{l_e\})}\delta_t^{\mathrm{E}}(\{l_e\})\\
+&\sum_{t\in\Delta:\text{ sl}}\abs{A_t(\{l_e\})}\delta_t^{\mathrm{L},\pm}(\{l_e\})\,,
\end{aligned}
\end{equation}
%
with $\abs{A_t}$ the absolute value of the triangle area. 
In the presence of a boundary, $\partial\Delta\neq\emptyset$, $\Sreg$ attains boundary terms such that overall additivity is ensured.
The equations of motion are given by $\pdv{\Sreg}{l_e} = 0$ supplemented by the Schl\"{a}fli identity, $\sum_t \abs{A_t}\pdv{\delta_t}{l_e} = 0$. 

Regge calculus can be formulated in terms of different variables~\cite{Barrett:1994nn,Dittrich:2008va}, with area variables~\cite{Asante:2018wqy,Makela:2000ej,Barrett:1997tx} playing a particularly important role here due to their connection to LQG and spin-foams. 
The relation between area and length variables is in general not invertible, and area variables need to be further constrained to yield the dynamical equations of length Regge calculus~\cite{Barrett:1997tx,Dittrich:2008va,Asante:2021zzh}.
However, in the symmetry reduced setting introduced below, the relation of area and length is globally invertible and the aforementioned constraints trivialize.
For the remainder, we study the classical dynamics of area Regge calculus which will be used in future work as a comparison to results of the effective spin-foam path integral.

\subsection{Lorentzian 4-frusta}\label{sec:Lorentzian frustum}

Lorentzian $4$-dimensional frusta are ideal to capture spatially flat, homogeneous and isotropic discrete geometries~\cite{Bahr:2017bn,Jercher:2023csk}.
The boundary of a $4$-frustum, depicted in Fig.~\ref{fig:4frustum}, contains two spacelike $3$-cubes and six $3$-frusta that are either spacelike or timelike.
Gluing $4$-frusta along boundary $3$-frusta in spatial direction and along boundary $3$-cubes in temporal direction, one obtains an extended cosmological spacetime of hypercubical combinatorics $\mathcal{X}^{(4)}_\mathcal{V}$ with slices labelled by $n\in\{0,...,\mathcal{V}_T\}$ discretized by $\mathcal{V}_S^3$ spacelike $3$-cubes.
We say that a $4$-frustum lies in a \enquote{slab} between slices $n$ and $n+1$.

The entire geometry of a slab is captured by three variables describing the $3$-cubes and $3$-frusta which can be either areas or lengths.
The former set of variables consists of the area of squares contained in the $3$-cubes, $j_0$ and $j_1$, and the area of the trapezoids contained in the $3$-frusta, $k$.
The latter consists of the edge length contained in the $3$-cubes, $s_0$ and $s_1$, and the height of the $4$-frustum, $H_0$, which implicitly determines the trapezoids and $3$-frusta.\footnote{Strictly speaking, the height $H_0$ is a derived quantity that is not a length associated to a $1$-dimensional sub-cell of the cellular complex. Such a quantity would instead be given by the length $l_0$ associated to the strut connecting two neighboring spacelike slices. However, the relation between strut length and height is invertible for non-null struts, which assume here.}
In the highly symmetry restricted setting considered here, there is a one-to-one map between area variables, $(j_0,j_1,k_0)$, and length variables, $(s_0,s_1,H_0)$, given explicitly in Appendix~\ref{app:frusta}.
We will exploit this relation in Sec.~\ref{sec:more time steps} using length variables for analytical computations and clear interpretation, especially because the height is directly related to the lapse function $N$ in the continuum.
When solving the Regge equations numerically thereafter, we will use area variables instead to provide a classical benchmark for the effective spin-foam calculations of~\cite{Jercher:2025def}.
The general notions of Regge calculus introduced above are straightforwardly adapted to either of the two sets of variables.


\begin{figure}
\includegraphics[width=0.28\textwidth]{figures/4frustum_high_res}
\caption{Boundary of a $4$-frustum given by two $3$-cubes and six equal $3$-frusta.}
\label{fig:4frustum}
\end{figure}

The squared volume of subcells connecting neighboring slices, i.e. 3-frusta, trapezoids and edges, which are detailed in Appendix~\ref{app:frusta}, carries information on the causal structure.
If it is positive (negative), the corresponding subcell is timelike (spacelike).
In the symmetry reduced setting, the ratio $\frac{\abs{j_n-j_{n+1}}}{4k_n}$ fully characterizes the causal character of subcells suggesting separating the $4$-frusta configurations into three sectors summarized in Tab.~\ref{tab:sectors}.

\setlength{\tabcolsep}{1.0em} % for the horizontal padding
{\renewcommand{\arraystretch}{1.5}
\begin{table*}
  \centering
  \begin{tabular}{c || c | c | c | c}
  sub-cells & $-\frac{\Delta j^2}{8} < \underline{k}^2 < -\frac{\Delta j^2}{16}$ & $-\frac{\Delta j^2}{16} < \underline{k}^2 < 0$ & $0 < \underline{k}^2 < \frac{\Delta j^2}{16}$ & $\frac{\Delta j^2}{16} <  \underline{k}^2$\\
  \hline\hline
  struts & spacelike & spacelike & spacelike & timelike \\
  \hline
  trapezoids & spacelike & spacelike & timelike & timelike\\
  \hline
  3-frusta & spacelike & timelike & timelike & timelike  
  \end{tabular}
  \caption{Causal characters of sub-cells as a function of $(j_0,j_1,k)$. The squared volumes demonstrating this are given in Appendix~\ref{app:frusta}.}
  \label{tab:sectors}
\end{table*}
}

As discussed in~\cite{Dittrich:2021gww,Dittrich:2023rcr,Jercher:2023csk,Jercher:2024kig,Jercher:2024hlr}, two of these sectors I and II exhibit an irregular light cone structure, also referred to as causality violations, located at spacelike sub-cells.
Examples of such irregularities are given by the trouser singularity and the yarmulke singularity associated to spatial topology change~\cite{Horowitz:1990qb,Louko:1995jw,Dowker:1999wu}. 
For causality violations at $(d-2)$-dimensional spacelike hinges the imaginary parts of the dihedral angles do not sum up to $\mp i2\pi$, leading to a complex valued Regge action.
Choosing \enquote{$+$} in the convention of Lorentzian angles, the amplitude $\e^{i\Sreg}$ is exponentially suppressed for trouser and exponentially enhanced for yarmulke configurations, vice versa for \enquote{$-$}~\cite{Asante:2021zzh}.%\footnote{It has been shown in~\cite{Jercher:2024kig,Jercher:2024hlr} that such an exponential suppression does not arise from the semi-classical limit of a single spin-foam frustum amplitude.}
Without a complexification of the variables, the Regge equations of motion for causality violating configurations are complex equations with real variables and therefore overconstrained.
Moreover, the results of~\cite{Jercher:2023csk,Dittrich:2021gww} show that in the respective settings, no real solutions exist.
Since this applies also to the setting used in this work we henceforth focus on the causally regular sector III.

\subsection{Lorentzian Regge action for spatially flat cosmology}\label{sec:Lorentzian Regge action for spatially flat cosmology}

For a single $4$-frustum between, say slice $n$ and $n+1$, the causally regular Regge action is given by
%
\begin{widetext}
\begin{equation}\label{eq:regular Regge action}
\begin{aligned}
S_{\textsc{iii}}^{(n)} & = 6(j_n-j_{n+1})\sinh^{-1}\left(\frac{j_{n+1}-j_n}{\sqrt{16k_n^2+(j_n-j_{n+1})^2}}\right) + 12k_n\left[\frac{\pi}{2}-\cos^{-1}\left(\frac{(j_n-j_{n+1})^2}{16k_n^2+(j_n-j_{n+1})^2}\right)\right] - \Lambda V^{(4)}\,,
\end{aligned}
\end{equation}  
\end{widetext}
%
where $\Lambda$ is a cosmological constant and $V^{(4)}$ is the $4$-volume defined in Eq.~\eqref{eq:4-volume} as an equation of $(j_n,j_{n+1},k_n)$.
By construction, the $4$-frustum action is additive such that on a discretization $\mathcal{X}^{(4)}_{\mathcal{V}}$ with $\mathcal{V}_S^3$ vertices in spatial direction and $\mathcal{V}_T+1$ spatial slices, the action is given by $S_{\mathrm{R}} = \mathcal{V}_S^3\sum_{n=0}^{\mathcal{V}_T-1}S_{\mathrm{R}}^{(n)}$.
Additivity of the action per slab implies that the equations of motion for a given geometric quantity, being either $j_n$ or $k_n$, will only depend on neighboring geometric labels.

In the following, we study the discrete dynamics of spatially flat cosmology as governed by the Regge equations. To that end, we consider first the case of a single time step, $\mathcal{V}_T = 1$, in Sec.~\ref{sec:recap} and generalize then to an arbitrary number of time steps in Sec.~\ref{sec:more time steps}.

\section{Recap: One time step}\label{sec:recap}

The classical and quantum dynamics of a single timestep in discrete Lorentzian cosmology have been studied extensively in~\cite{Dittrich:2021gww,Dittrich:2023rcr,Asante:2021phx} for spatial curvature with cosmological constant and in~\cite{Jercher:2023csk,Jercher:2024hlr} for spatially flat spacetimes with a free scalar field.
In this chapter we extend these studies to spatially flat cosmology with a cosmological constant, before then focussing on the case of arbitrarily many time steps. 

For $\mathcal{V}_T = 1$, the square areas $j_0,j_1$ are boundary data and only the trapezoid area $k_0$ is dynamical. Thus, the Regge equations are simply given by
%
\begin{equation}\label{eq:1step Regge equation}
\pdv{S_{\textsc{iii}}}{k_0} = 0\,,
\end{equation}
%
%\begin{widetext}
%\begin{equation}\label{eq:1step Regge equation}
%0 = \pdv{S_{\textsc{iii}}}{k_0} = 12k_0\left[\frac{\pi}{2}-\cos^{-1}\left(\frac{(j_0-j_{1})^2}{16k_0^2+(j_1-j_{1})^2}\right)\right] - \Lambda \frac{(j_0 + j_1)^2k}{4 V^{(4)}}\,.
%\end{equation}
%\end{widetext}
%
where due to the Schl\"{a}fli identity, the first term in Eq.~\eqref{eq:regular Regge action} drops out. 
Notice that this equation is transcendental and can therefore only by tackled numerically.

\paragraph*{Vanishing cosmological constant.} 

In the case of $\Lambda = 0$, the Regge action describes a spatially flat vacuum universe. 
If the boundary data is non-stationary, i.e.~$j_0 \neq j_1$, Eq.~\eqref{eq:1step Regge equation} exhibits no solution for $k_0$. 
In other words, a classical universe can neither expand nor contract if there is no matter present. 
This is in accordance with the continuum Friedmann equations in vacuum, $\left(\frac{\dot{a}}{a}\right)^2 = 0$, with $a$ the scale factor, which are solved only by $a = \mathrm{const}$.
On the other hand, if $j_0 = j_1$, Eq.~\eqref{eq:1step Regge equation}  is trivially satisfied and the trapezoid area $k_0$ is undetermined.
In fact, the Regge action vanishes, $S_{\textsc{iii}} = 0$ for all $k_0$, and the $4$-frusta reduce to $4$-cuboids discretizing flat space. 
The arbitrariness of $k$ is a common phenomenon for flat solutions in Regge calculus~\cite{Rocek:1981ama,Rocek:1982tj} and signifies the restoration of diffeomorphism symmetry in the discrete~\cite{Dittrich:2008pw,Bahr:2009qc}.
This symmetry amounts to moving spacelike slices (instead of individual vertices), see also the discussion in~\cite{Bahr:2015gxa}.
Note that for $4$-cuboids, $k = \sqrt{j}H$ with $H$ the height of the cuboid which is directly related to the lapse function $N$ in the continuum~\cite{Dittrich:2021gww,Jercher:2023csk}.
The lapse is the gauge variable of the residual diffeomorphism symmetry after symmetry reduction, encoding time reparametrization invariance. 

\paragraph*{Non-vanishing cosmological constant.}

For a non-vanishing cosmological constant, $\Lambda \neq 0$, and stationary boundary conditions, $j_0 = j_1$, the only solution to Eq.~\eqref{eq:1step Regge equation} is $k_0 = 0$, corresponding to degenerate $4$-frusta with vanishing height.
Thus, a stationary universe for $\Lambda\neq 0$ only exists if no time passes, i.e. if there is no actual time evolution. 
The more interesting case is $j_0 \neq j_1$ where a non-trivial solution $k_0^*$ for Eq.~\eqref{eq:1step Regge equation} exists. 
More precisely, a unique solution $k_0^*$ is found for any $\Lambda > 0$ and any $j_0\neq j_1$. 
The on-shell trapezoid area $k_0^*(\Lambda)$ as a function of $\Lambda$ behaves as a monomial decay $k_0^*\sim c\Lambda^{-\alpha}$ with $c,\alpha > 0$ constants depending on $j_0, j_1$. 
For $\Lambda \neq 0$, the solution diverges and ceases to exist at $\Lambda = 0$ in lines with the discussion above.
In the limit $\Lambda\rightarrow\infty$, Eq.~\eqref{eq:1step Regge equation} is dominated by the $\Lambda$-term, i.e.
%
\begin{equation}
\Lambda \frac{(j_0 + j_1)^2k}{4 V^{(4)}} \approx 0\,,
\end{equation}
%
solved by $k_0^* = 0$.

Note that the restriction on the sign of $\Lambda$ is in accordance with the continuum Friedmann equations, $\left(\frac{\dot{a}}{a}\right)^2 = N^2\frac{\Lambda}{3}$, where real solutions for the scale factor require $\Lambda > 0$. 

For completeness, let us contrast these results with the existence of solutions in the spatially spherical case in Lorentzian~\cite{Dittrich:2021gww} and Euclidean~\cite{Hilpert2024,Collins:1973hw} signature.\footnote{The construction of the spatially spherical Regge action differs from the construction here, in particular because there exist only finitely many homogeneous triangulations of the $3$-sphere, see~\cite{Collins:1973hw,Dittrich:2021gww} for details.} 
In the former setting, note first that for $\Lambda = 0$, no solutions exist. 
Furthermore, for $\Lambda > 0$, solutions exist only if $j_0 > j_\Lambda$ and $j_1 > j_\Lambda$, where $j_\Lambda$ is a $\Lambda$-dependent minimal area. 
This is in accordance with the continuum where the scale factor typically follows a $\cosh$-function, attaining a minimal value $a_\Lambda$.
In Euclidean signature, solutions exist for $\Lambda = 0$. 
These correspond, in the continuum, to solutions where $\dot{a} = \mathrm{const}$.
For $\Lambda > 0$, solutions exist only if $j_0 < j_\Lambda$ and $j_1<j_\Lambda$ where $j_\Lambda$ is the same as in the Lorentzian case, however now representing an upper bound. 
Again, this behavior agrees with continuum results where a Euclidean universe with positive spatial curvature can only reach a maximum value $a_\Lambda$.
 
\section{Arbitrary number of time steps}\label{sec:more time steps}

In this section, we study the Regge equations of spatially flat Lorentzian cosmology for an arbitrary number of time steps $\mathcal{V}_T$. 
This amounts to solving $2\mathcal{V}_T-1$ coupled transcendental equations for the bulk variables $k_0,j_1,\dots,j_{\mathcal{V}_T-1},k_{\mathcal{V}_T-1}$ for given boundary data $j_0, j_{\mathcal{V}_T}$ and cosmological constant $\Lambda$,
%
\begin{equation}\label{eq:general Regge equations}
\pdv{S_{\textsc{iii}}}{k_n} = 0\,,\qquad \pdv{S_{\textsc{iii}}}{j_m} = 0\,,
\end{equation}
%
with $n\in\{0,\dots,\mathcal{V}_T-1\}$ and $m\in\{1,\dots,\mathcal{V}_T-1\}$. Since the Regge action $S_\textsc{iii}$ is additive with respect to the slices, i.e. $S_\textsc{iii} = \mathcal{V}_S^3\sum_n S_\textsc{iii}^{(n)}$, Eqs.~\eqref{eq:general Regge equations} simplify significantly,
%
\begin{equation}
\begin{aligned}
0 &= \pdv{S_\textsc{iii}}{k_n} = \mathcal{V}_S^3\pdv{S_\textsc{iii}^{(n)}}{k_n} \,,\\
0 &= \pdv{S_\textsc{iii}}{j_n} = \mathcal{V}_S^3\left(\pdv{S_\textsc{iii}^{(n)}}{j_n} + \pdv{S_\textsc{iii}^{(n+1)}}{j_n}\right)\,.
\end{aligned}
\end{equation}
%
Thus, it is sufficient to consider the single slab action $S^{(n)}_\textsc{iii}$, given in Eq.~\eqref{eq:1step Regge equation}, and its derivatives. An explicit form of these equations is given in Appendix~\ref{app:frusta}. 

A priori, the number of cubes in each spacelike slice, $\mathcal{V}_S^3$, enters the total Regge action only as a multiplicative factor due to the symmetry restriction.
Thus, in the following two sections, we consider $\mathcal{V}_S = 1$ for simplicity.
However, let us mention here that in Sec.~\ref{sec:continuum limit}, the parameter $\mathcal{V}_S$ turns out to be crucial to obtain the correct continuum limit. 
We tackle the problem of finding solutions to Eq.~\eqref{eq:general Regge equations} first analytically in the subsequent section and develop numerical methods thereafter.

\subsection{Analytical considerations}\label{sec:analytics}

For vanishing cosmological constant, $\Lambda = 0$, and stationary boundary data, $j_0=j_{\mathcal{V}_T}$, the results for a single time step obtained in the previous section straightforwardly generalize.
That is, if $\Lambda = 0$, the Regge equations are satisfied if and only if the boundary data satisfies $j_0 = j_{\mathcal{V}_T}$.
Then, the bulk areas $j_n = j_0$ for all $n\in\{1,\dots,\mathcal{V}_T-1\}$, and the trapezoid areas are undetermined, marking the restoration of time reparametrization invariance of flat space. 
On the other hand, for $\Lambda \neq 0$ and boundary data $j_0 = j_{\mathcal{V}_T}$, no non-degenerate solution to the Regge equations is found.
A unique solution exist for $j_0 \neq j_{\mathcal{V}_T}$ when $\Lambda > 0$ which is in accordance with the continuum Friedmann equations.
To summarize, a spatially flat vacuum universe is flat and stationary while a positive cosmological will necessarily lead to curved non-stationary solutions. 
For the remainder, we assume $\Lambda > 0$ and $j_0\neq j_{\mathcal{V}_T}$.

Eq.~\eqref{eq:general Regge equations} has been analyzed further (coupling a scalar field rather than a cosmological constant) in~\cite{Jercher:2023csk}.
Therein, the length variables $(s_n,s_{n+1},H_n)$ have been employed in which the monotonicity of solutions and the continuum limit are derived most easily.
The results of~\cite{Jercher:2023csk} transferred to the present setting with $\Lambda\neq 0$ are briefly summarized in the following.

Combining the Regge equations
%
\begin{equation}
\pdv{S_{\textsc{iii}}}{H_{n-1}} = 0\,,\quad \pdv{S_{\textsc{iii}}}{H_{n}} = 0\,,\quad \pdv{S_{\textsc{iii}}}{s_{n}} = 0\,,
\end{equation}
%
one can straightforwardly derive the relation~\cite{Jercher:2023csk}
%
\begin{equation}\label{eq:length difference}
\frac{s_n - s_{n-1}}{H_{n-1}} = \frac{s_{n+1} - s_n}{H_n}\,,
\end{equation}
%
for all $n\in\{1,\dots, \mathcal{V}_T-1\}$.
That is, the difference of spatial edge length with respect to the height of the $4$-frusta remains constant. 
Introducing 
%
\begin{equation}
\tau_0 = \sum_{n=0}^{\mathcal{V}_T-1}H_n\,,
\end{equation}
%
which we will later identify as the discrete elapsed proper time,  one can show that Eq.~\eqref{eq:length difference} implies
%
\begin{equation}
\frac{s_{n+1}-s_n}{H_n} = \frac{s_{\mathcal{V}_T} - s_0}{\tau_0}\,,
\end{equation}
%
for all $n$.
Since $H_n,\tau_0 > 0$, this implies $\mathrm{sgn}(s_{n+1}-s_n) = \mathrm{sgn}(s_{\mathcal{V}_T}-s_0)$, that is, solutions evolve monotonically and the boundary data $s_0,s_{\mathcal{V}_T}$ selects whether the universe is expanding, $s_{\mathcal{V}_T}>s_0$, or contracting, $s_{\mathcal{V}_T} < s_0$.

Following the procedure outlined in~\cite{Jercher:2023csk}, an analytical continuum limit of the Regge equations can be taken in two steps.
First, one performs a linearization of $\pdv{S_{\textsc{iii}}}{H_n} = 0$ in the variable $\eta_n = \frac{s_{n+1} - s_n}{H_n}$.
In this approximation, corresponding to small deficit angles, one finds
%
\begin{equation}\label{eq:discrete Friedmann}
\frac{(s_n - s_{n+1})^2}{s_n^2+s_{n+1}^2}  = H_n^2\frac{\Lambda}{6}\,.
\end{equation}
%
Then, the discrete variables $(s_n,s_{n+1},H_n)$ need to be identified with the continuum scale factor $a(t)$ and the lapse function $N(t)$ parametrized in some coordinate time $t$.
In the continuum limit, the height $H_n$ corresponds to the infinitesimal proper time, $H_n\rightarrow N\mathrm{d}t$.
The discrete length on a given slice is mapped to the scale factor, $s_n \rightarrow a(t)$, while the length of a neighboring edge attains an additional time derivative term, $s_{n+1}\rightarrow a(t) + \dot{a}\mathrm{d}t$.
Then, at leading order in $\mathrm{d}t$, on finds indeed that Eq.~\eqref{eq:discrete Friedmann} approaches
%
\begin{equation}
\left(\frac{\dot{a}}{a}\right)^2 = N^2\frac{\Lambda}{3}\,,
\end{equation}
%
which is the Friedmann equation for a spatially flat universe with cosmological constant.
Note that configurations with spacelike trapezoids or spacelike $3$-frusta satisfy $\eta_n^2>2$ and therefore bear no straightforward continuum limit~\cite{Jercher:2023csk}.

The cosmological Regge equations have been shown to capture  the Friedmann equations in a suitable continuum limit in previous works~\cite{Jercher:2023csk,Dittrich:2021gww,Bahr:2017bn,Brewin1999}.
Still, Eq.~\eqref{eq:general Regge equations} remain to be solved explicitly in the discrete for many time steps $\mathcal{V}_T$.
This represents however an important step since the solutions serve as a classical and discrete benchmark for identifying a semi-classical continuum regime in discrete path integral approaches such as effective spin-foams.  
In the following section, we tackle this issue employing numerical methods.

\subsection{Numerical implementation}

Solving the Regge equations explicitly requires numerical methods. In this work, we employ the package \texttt{NLsolve}\footnote{
A documentation of the package can be found at \href{https://github.com/JuliaNLSolvers/NLsolve.jl}{github.com/JuliaNLSolvers/NLsolve.jl}. 
Note that also the \texttt{NSolve} method in \texttt{Mathematica} can be used, but is expected have lower performance in terms of accuracy and computation time.
}
in \texttt{Julia}, designed for solving non-linear equations.
Note that while Eqs.~\eqref{eq:general Regge equations} define a real-valued function in real variables, \texttt{NLsolve} can also be applied to complex numbers \caj{which may be relevant for the complex critical point method~\cite{}.}
The algorithm we developed for this work specifically tailored to Eq.~\eqref{eq:general Regge equations} amounts to the \texttt{solve\_eom}\footnote{See \href{https://github.com/Jercheal/transfer_matrix/blob/main/transfer_matrix.jl}{this github repository} for the source code.} method which we explain in the following.

First, note that the method \texttt{nlsolve} of the \texttt{NLsolve} package takes as input a vector valued function $\vec{F}$, a vector of initial variables $\vec{x}_0$, and parameters for the number of iterations as well as the error tolerance for solutions. 
If the problem is sufficiently well-behaved, \texttt{nlsolve} finds a solution to the equation $\vec{F}(\vec{x}) = \vec{0}$ using $\vec{x}_0$ as a starting guess of the solving algorithm. 
In our case, $\vec{F}:\R^{2\mathcal{V}_T-1}\longrightarrow \R^{2\mathcal{V}_T-1}$, with
%
\begin{align}
\vec{F} &= \left(\pdv{S_{\textsc{iii}}}{k_0},\pdv{S_{\textsc{iii}}}{j_1},\dots,\pdv{S_{\textsc{iii}}}{j_{\mathcal{V}_T-1}},\pdv{S_{\textsc{iii}}}{k_{\mathcal{V}_T-1}}\right)\,,\\[7pt]
\vec{x} &= \left(k_0,j_1,\dots,j_{\mathcal{V}_T-1},k_{\mathcal{V}_T-1}\right)\,.
\end{align}
%
Therefore, adapting Eq.~\eqref{eq:general Regge equations} to be solved by \texttt{nlsolve} is done in two steps: 1) set up the function $\vec{F}$ for fixed boundary areas $j_0,j_{\mathcal{V}_T}$ and cosmological constant $\Lambda$, and 2) propose a starting guess $\vec{x}_0$.
While step 1) is straightforward, step 2) requires particular care because the starting guess $\vec{x}_0$ is crucial for convergence and good performance of the solving algorithm.
In particular, as $\mathcal{V}_T$ grows, the number of variables and equation grows and the algorithm becomes more sensitive to the choice of $\vec{x}_0$. 

By trial and error, one can find sufficient starting guesses for \texttt{nlsolve} to converge given various small $\mathcal{V}_T$.\footnote{We tried using the continuum solution to find a good initial guess. However, we found that the prescription given here ensures fast convergence more reliably.} 
For the solutions $\vec{x}^* = (k_0^*,j_1^*,\dots,j_{\mathcal{V}_T-1}^*,k_{\mathcal{V}_T-1}^*)$, the following patterns are being observed.
First, the $j^*_n$ can be approximated as
%
\begin{equation}\label{eq:j_guess}
j^*_n \approx j_0 + n\Delta j\,,\quad n\in\{1,\dots,\mathcal{V}_T-1\}\,,
\end{equation}
%
with $\Delta j = (j_{\mathcal{V}_T} - j_0)/\mathcal{V}_T$.
Second, let $\vb*{k}_0^*$ be the solution of the trapezoid area for $\mathcal{V}_T = 1$.
Then, for $\mathcal{V}_T > 1$, the $k^*_n$ are approximated by 
%
\begin{equation}\label{eq:k guess}
k^*_n \approx \vb*{k}^*_0/\mathcal{V}_T\,, \quad n\in\{0,\dots,\mathcal{V}_T-1\}\,.
\end{equation}
%
That is, to find a good initial guess for the trapezoid areas, one only needs to find a solution $\vb*{k}_0^*$ for a single time step. 
Crucially, for a single equation, \texttt{nsolve} converges for a broad range of initial guesses, so $\vb*{k}_0^*$ can be easily found.
In summary, finding a good initial guess $\vec{x}_0$ amounts to finding the one time step solution $\vb*{k}_0^*$ and then setting
%
\begin{equation}
\vec{x}_0 = \left(\frac{\vb*{k}_0^*}{\mathcal{V}_T},j_0+\Delta j,\dots, j_0+(\mathcal{V}_T-1)\Delta j, \frac{\vb*{k}_0^*}{\mathcal{V}_T}\right)\,.
\end{equation}

Implementing this setup, the function \texttt{solve\_eom} takes as input the boundary data $j_0,j_{\mathcal{V}_T}$, the cosmological constant $\Lambda$, a parameter $\gamma = 1$\footnote{In future work, when comparing effective spin-foam results with the classical solutions, $\gamma\in\R$ will be allowed to vary, corresponding to the Barbero-Immirzi paramter~\cite{}.} and solver-specific arguments such as the maximum number of iterations or  error tolerances.
The output of \texttt{solve\_eom} is a tuple consisting of four arrays: the $\mathcal{V}_T+1$ square area solutions $j^*_n$, including the boundary values, the $\mathcal{V}_T$ trapezoid areas $k^*_n$, the heights of $4$-frusta $H^*_n$ and the accumulated heights $\tau_n = \sum_{k=0}^nH^*_k$.

In the following, we discuss the numerical stability and time complexity of the algorithm before then applying \texttt{solve\_eom} to study the properties of discrete solutions and their continuum limit.

\subsection{Numerical stability and time complexity}

The performance of the solving algorithm is not only measured by its ability to find solutions but also by its stability and its time efficiency.
As discussed in the previous section, the initial guess $\vec{x}_0$ is crucial for the performance of \texttt{nlsolve}.
Therefore, we will check first, whether \texttt{solve\_eom} is stable under perturbations $\vec{\delta}$ of $\vec{x}_0$.
Second, we estimate the scaling of computation time with the number of time steps $\mathcal{V}_T$.
This is particularly important for later studies of the continuum limit, where access to large $\mathcal{V}_T$ is of physical interest.

To understand the behavior of the solver under perturbations of the initial guess $\vec{x}_0$, consider a vector $\vec{\delta} = \frac{1}{\sqrt{2\mathcal{V}_T - 1}}\left(\delta,\dots,\delta\right)\in\R^{2\mathcal{V}_T-1}$.
Then, \texttt{solve\_eom} with initial guess $\vec{x}_0+\vec{\delta}$ is evaluated leading to the following results.
\paragraph*{$\mathcal{V}_T=1$.} \texttt{solve\_eom} yields consistent results as long as the entries of $\vec{x}_0+\vec{\delta}$ are positive.
For negative entries, there are non-trivial sign changes in the Regge equation, and in this case, it not clear if solutions even exist.
Choosing $\delta$ such that the entries are positive, the solutions are stable even for large $\delta > 100$. 
Also, the number of iterations does only increase slightly.
This is important since setting up $\vec{x}_0$ requires finding $\vb*{k}_0^*$ first.
If this first step would be extremely sensitive, the proposal above would not be viable.
\paragraph*{$\mathcal{V}_t > 1$.} In this case, the number of iterations needed to find convergence is generically larger, even for $\delta = 0$.
Choosing $\delta$ such that the entries of $\vec{x}_0+\vec{\delta}$ are positive, one finds that solutions are stable for small $\abs{\delta}$ ($\abs{\delta}\lesssim 10$ for $\mathcal{V}_T=2$) with little fluctuations in the required iterations.
However, for larger $\delta$, the solutions suddenly jump to unreasonably large or to negative values and the number of iterations increases.
This effect worsens with $\mathcal{V}_T$.
For the solver, this means that 1) the proposal of $\vec{x}_0$ given above is already optimal and one should stick with it, and 2) for very large $\mathcal{V}_T$, the region of trustworthy convergence might shrink to zero and one needs to come up with a refined proposal of initial guesses as $\vec{x}_0$.
In this work, the values we consider for $\mathcal{V}_T$ are small enough for $\vec{x}_0$ to yield satisfactory results.

To measure how computation time scales with the number of time steps, $\mathcal{V}_T$ (and thus with the number of equations, $2\mathcal{V}_T-1$), we fix the boundary data $j_0,j_{\mathcal{V}_T}$ and $\Lambda$ and apply \texttt{solve\_eom} with increasing $\mathcal{V}_T$.
In the $\log$-$\log$-plot of Fig.~\ref{fig:time scaling}, one observes a monomial scaling. 
Performing a linear regression, one find that the computation time as a function $\mathcal{V}_T$ can be approximated as $t(\mathcal{V}_T) = 10^{\hat{\alpha}}V^{\hat{\beta}}$ with $\hat{\alpha} = -6.67$ and $\hat{\beta} = 4.23$.
That is, the computation time for small $\mathcal{V}_T$ is very small, $\sim 10^{-6}\mathrm{s}$, but increases as a fourth-order polynomial in $\mathcal{V}_T$.
Still, the times at $\mathcal{V}\sim 10^2$ are reasonably small so that the behavior of solutions in the continuum limit, tackled in the next section, can be studied without any computation time issues.  

\begin{figure}
\includegraphics[width=0.45\textwidth]{plots/solver_timescaling}
\caption{Logarithm of computation time as a function of $\log\mathcal{V}_T$. The fit via linear regression yields $t(V) = 10^{\hat{\alpha}} V^{\hat{\beta}}$ with $\hat{\alpha} = -6.67$ and $\hat{\beta} = 4.23$. For demonstrative purposes, the boundary data and $\Lambda$ have been fixed to $j_0 = 50, j_{\mathcal{V}_T} = 100$ and $\Lambda = 0.1$.} 
\label{fig:time scaling}
\end{figure}

\section{Solutions and the continuum limit}\label{sec:continuum limit}

Facilitated by the numerical tools developed above, we study in this section the continuum limit of Regge calculus at the level of the solutions, rather than at the level of the equations.
Quantifying whether the discrete solutions provide a satisfactory approximation to the continuum ones requires two ingredients:
1) For comparison, one needs to consider appropriate continuum quantities which serve as limiting points of the discrete solutions.
2) One needs to identify the right parameters to tune. 
Here, the discreteness is parametrized by the number of slices, $\mathcal{V}_T$, as well as the number of $3$-cubes per slice, $\mathcal{V}_S^3$.
Thus, the continuum is expected to be reached by scaling $\mathcal{V}_T,\mathcal{V}_S\rightarrow\infty$ in a suitably combined way.

\paragraph*{1) Continuum quantities.} One difficulty in comparing discrete and continuum solutions is that in the discrete, the trapezoid areas are dynamical while in the continuum, the lapse function $N$ is a gauge variable, and thus, any time parametrization corresponds to a gauge choice.
That is, when performing the limit, which time parametrization in the continuum emerges?
Clearly, the labels $n\in\{0,\dots,\mathcal{V}_T\}$ do not serve as a time as this would imply constant distance between any two neighboring spacelike slices.
Instead, recall that the height of $4$-frusta was mapped to infinitesimal proper time in the analytical continuum limit of Sec.~\ref{sec:analytics}.
Therefore, the sum of heights, $\tau_0 = \sum_n H_n$, serves as a discrete approximation of the continuum proper time elapsed, defined as $\int \dd{t}N(t)$.
The advantage of considering proper time is that it is gauge invariant.
Thus, one does not run into the issue of having to pick a parametrization in the continuum to compare to the discrete solutions.
Using proper time, we will then compare the evolution of the square area $j^*_n$ as a function of the accumulated height $\sum_{m=0}^nH_{m}$ with the squared scale factor $A = a^2$ as a function of proper time, $A(\tau)$.

\paragraph*{2) The right scaling.} A priori, the parameter $\mathcal{V}_S$ enters the Regge action only as a multiplicative factor.
Therefore, it might be tempting to set $\mathcal{V}_S = 1$, as done in the analytical part in Sec.~\ref{sec:analytics}.
Note, however, that when describing the same total boundary area $j_0,j_{\mathcal{V}_T}$ with a different number of spacelike building blocks, a re-scaling in the $4$-frustum Regge action is required,
%
\begin{equation}
S_{\textsc{iii}}^{(n)}(j_n, j_{n+1}, k_n) \rightarrow \mathcal{V}_S^3 S_{\textsc{iii}}^{(n)}(j_n/\mathcal{V}_S^2, j_{n+1}/\mathcal{V}_S^2, k_n)\,.
\end{equation}
%
This refinement in spacelike direction is depicted in Fig.~\ref{fig:spatial refinement}.
Importantly, the Regge action and thus also the solutions, are not invariant under the procedure above.
Thus, different combined scalings of $\mathcal{V}_S,\mathcal{V}_T\rightarrow \infty$ need to be considered to find convergence to continuum solutions.

\begin{figure}
\centering
\includegraphics[width=0.45\textwidth]{figures/refinement.pdf}
\caption{Refinement of frustum geometry with total initial square area $j_0$ and final square area $j_1$. Individual frusta are rescaled as $(j_0,j_1)\rightarrow (j_0/\mathcal{V}_S^2, j_1/\mathcal{V}_S^2)$. For illustrative purposes, one spatial dimension is suppressed.}
\label{fig:spatial refinement}
\end{figure}

However, Fig.~\ref{fig:continuum scalings} demonstrates that leaving $\mathcal{V}_S = 1$ while increasing $\mathcal{V}_T$, the accumulated height $\tau = \sum H$ does not converge to the value of the continuum proper time $\tau_0$.
To obtain the correct $\tau_0$, one therefore needs to tune $\mathcal{V}_S$ and $\mathcal{V}_T$ simultaneously.
Here, a second subtlety needs to be noticed.
The continuum proper time only depends on $\Lambda$ and the initial and final squared scale factor $A_0, A_1$.
Setting $j_0 = A_0$ and $j_{\mathcal{V}_T} = A_1$ and increasing $\mathcal{V}_S$ i

$\mathcal{V}_T$ and $\mathcal{V}_S$ can be scaled to larger values individually.
In particular, from Eq.~\eqref{eq:general Regge equations}, one can see that 

\caj{Continuum limit also in the analytical sense? Consider $s_n\rightarrow s_n/\mathcal{V}_S$.}

\begin{figure}
\centering
\includegraphics[width=0.45\textwidth]{plots/continuum_scalings.pdf}
\caption{.}
\label{fig:continuum scalings}
\end{figure}

\subsection{Perfect action for Lorentzian Regge cosmology}



\section{Conclusion}

Extension to $k = \pm 1$. 

\paragraph*{Acknowledgements}  The authors thank Seth Asante, Bianca Dittrich and Steffen Gielen for helpful discussions and Tim St\"{o}tzel for support with the numerical implementation. AFJ, JDS and SSt gratefully acknowledge support by the Deutsche Forschungsgemeinschaft (DFG, German Research Foundation) Grant No 422809950. AFJ and JDS acknowledge support by the DFG under Grant No 406116891 within the Research Training Group RTG 2522/1. AFJ is grateful for the generous financial support by the MCQST via the seed funding Aost 862981-8 granted to Jibril Ben Achour by the DFG under Germany’s Excellence Strategy – EXC-2111 – 390814868.

\appendix
\section{Frusta geometry}\label{app:frusta}

In area variables, the geometry of a slab is fully characterized by $(j_0, j_1,k)$, the square area of the cubes in slice $0$ and $1$, respectively, and the trapezoid area $k$.
We provide the formulae of the squared volumes of relevant sub-cells in the following.
Here, the square areas $j_0,j_1$ are assumed to be the absolute values of the signed areas and therefore positive.
For the squared trapezoid area, we write $\underline{k}^2$ for the signed squared area and $k^2$ for its absolute value.

A crucial quantity is the squared height of the $4$-frusta,
%
\begin{equation}
H^2 = \frac{8\underline{k}^2 + (j_0-j_1)^2}{2(\sqrt{j_0} + \sqrt{j_1})^2}\,,
\end{equation}
%
due to its direct relation to the proper time in the continuum.
Furthermore, it gives a bound $8\underline{k}^2 > -(j_0 - j_1)^2$ for the $4$-frusta to be embeddable in Minkowski space.
Configurations that violate this bound correspond to Euclidean frusta.

The squared length of struts, which connect neighboring spacelike slices is
%
\begin{equation}
l^2 = \frac{16\underline{k}^2-(j_0-j_1)^2}{4(\sqrt{j_0} + \sqrt{j_1})^2}\,.
\end{equation}
%
Thus, configurations with $16\underline{k}^2 > (j_0-j_1)^2$ have timelike struts. 
If equality holds instead, struts are lightlike. 
Otherwise, they are spacelike.
The squared height of $3$-frusta is
%
\begin{equation}
h^2 = \frac{16\underline{k}^2 + (j_0-j_1)^2}{4(\sqrt{j_0} + \sqrt{j_1})^2}\,,
\end{equation}
%
so $3$-frusta are timelike if $16\underline{k}^2>-(j_0-j_1)^2$, lightlike if equality holds and spacelike otherwise.

The $4$-volume enters the Regge action with a cosmological constant and is the discrete analogon of the volume element $\dd[4]{x}\sqrt{-g}$,
% 
\begin{equation}\label{eq:4-volume}
V_{(4)}^2 = \frac{(j_0+j_1)^2}{32}\left(8\underline{k}^2 + (j_0-j_1)^2\right)\,.
\end{equation}
%

In this work, we focus exclusively on the causally regular sector, that is, timelike trapezoids, $\underline{k}^2 > 0$. 
The two types of deficit angles, those located at trapezoids and those located at squares, are respectively given by
%
\begin{equation}
\delta^{\mathrm{E}} = \frac{\pi}{2} - \cos^{-1}\left(\frac{(j_n - j_{n+1})^2}{16k^2 + (j_n - j_{n+1})^2}\right)\,,
\end{equation}
%
and 
%
\begin{equation}
\delta^{\mathrm{L}} = \sinh^{-1}\left(\frac{j_1-j_0}{\sqrt{16k^2 + (j_n - j_{n+1})^2}}\right)\,.
\end{equation}
%
$\delta^{\mathrm{E}}$ is a Euclidean angle since the orthogonal subspace to a timelike trapezoid is $\R^2$.
In contrast, $\delta^{\mathrm{L}}$ is a Lorentzian angle as the orthogonal subspace to a spacelike square is $\R^{1,1}$.

Let us repeat here the Regge action of the causally regular sector,
\begin{widetext}
\begin{equation}
\begin{aligned}
S_{\textsc{iii}}^{(n)} & = 6(j_n-j_{n+1})\sinh^{-1}\left(\frac{j_{n+1}-j_n}{\sqrt{16k_n^2+(j_n-j_{n+1})^2}}\right) + 12k_n\left[\frac{\pi}{2}-\cos^{-1}\left(\frac{(j_n-j_{n+1})^2}{16k_n^2+(j_n-j_{n+1})^2}\right)\right] - \Lambda V^{(4)}\,.
\end{aligned}
\end{equation}  
\end{widetext}
The equations of motion are given by
%
\begin{equation}
\pdv{S_{\textsc{iii}}}{k_n} = 0\,,\quad \pdv{S_{\textsc{iii}}}{j_n} = 0\,,
\end{equation}
%
which can be simplified using the Schl\"{a}fli identity and the additivity of the action.
That is, the above equations can be reformulated as
%
\begin{equation}
\pdv{S_{\textsc{iii}}^{(n)}}{k_n} = 0\,,\quad \pdv{S_{\textsc{iii}}^{(n-1)}}{j_n} + \pdv{S_{\textsc{iii}}^{(n)}}{j_n} = 0\,,
\end{equation}
with the derivative terms explicitly given by
\begin{widetext}
\begin{equation}
\begin{aligned}
\pdv{S_{\textsc{iii}}^{(n)}}{k_n} &= 12\left[\frac{\pi}{2} - \cos^{-1}\left(\frac{(j_n - j_{n+1})^2}{16k^2 + (j_n - j_{n+1})^2}\right)\right] - \Lambda \frac{(j_n+j_{n+1})^2k_n}{4V_{(4)}}\,,\\
\pdv{S_{\textsc{iii}}^{(n)}}{j_n} &= 6\sinh^{-1}\left(\frac{j_{n+1}-j_n}{\sqrt{16k_n^2 + (j_n - j_{n+1})^2}}\right) - \Lambda \frac{j_n(j_n - j_{n+1}) + 4k_n^2}{2\sqrt{(j_n - j_{n+1})^2+8k_n^2}}\,.
\end{aligned}
\end{equation}
\end{widetext}

\section{Cosmology in the continuum}

The line element of a spatially flat, homogeneous and isotropic universe is given by
%
\begin{equation}
\dd{s}^2 = N^2(t)\dd{t}^2 - a^2(t)\dd{\vb{x}}^2\,,
\end{equation}
%
with $a(t)$ the scale factor and $\dd{\vb{x}}^2$ the 3-dimensional Euclidean line element. 
The lapse function, $N(t)$, corresponds to a choice of gauge emerging from the time reparametrization invariance of the system. 

Dynamics of the cosmological system are governed by the Friedmann equations which, in the case where matter only consists of a cosmological constant, are simply given by
%
\begin{equation}\label{eq:Friedmann}
\left(\frac{\dot{a}}{a}\right)^2 = N^2\frac{\Lambda}{3}\,.
\end{equation}
%
This is straightforwardly solved by
%
\begin{equation}
a(t) = a_0 \exp\left(\pm\frac{\Lambda}{3}\int_{t_0}^{t}\dd{t'}N(t')\right)\,,
\end{equation}
%
where we identify the gauge-invariant elapsed proper time as
%
\begin{equation}
\tau^{\mathrm{cont}}(t_0,t) = \int_{t_0}^{t}\dd{t'}N(t')\,.
\end{equation}
%
The proper time plays an important role in the comparison between discrete and continuum equations as the cumulative height of frusta converges to the proper time in an appropriate continuum limit.

In the discrete, as well as for effective spin-foams in ensuing studies, one is not given an initial scale factor and an initial time but rather an initial and final scale factor (corresponding to $\sqrt{j_0}$ and $\sqrt{j_{\mathcal{V}_T}}$, respectively).
If, in the continuum, $a_0,a_1$ and $\Lambda$ are given, the proper time is fixed to
%
\begin{equation}\label{eq:continuum prop time}
\tau_0^{\mathrm{cont}} = \log\left(\frac{a_1}{a_0}\right)\sqrt{\frac{\Lambda}{3}}\,.
\end{equation}
%
This quantity serves as a limiting value of $\tau = \sum_n H_n$ in the continuum limit described in Sec.~\ref{sec:continuum limit}.









\bibliographystyle{JHEP}
\bibliography{references}

\end{document}


